steps:
  # Build the container image using base image from GCR
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'BASE_IMAGE=gcr.io/$PROJECT_ID/ggcraft-vue-base:latest'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ggcraft-front:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ggcraft-front:latest'
      - '.'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/ggcraft-front:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/ggcraft-front:latest'

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'ggcraft-front'
      - '--image'
      - 'gcr.io/$PROJECT_ID/ggcraft-front:$COMMIT_SHA'
      - '--region'
      - 'asia-east1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'VITE_API_URL=https://ggcraft-api-xxxxx.a.run.app/api'
      - '--max-instances'
      - '10'
      - '--min-instances'
      - '0'
      - '--memory'
      - '256Mi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '60'

images:
  - 'gcr.io/$PROJECT_ID/ggcraft-front:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/ggcraft-front:latest'

timeout: '1200s'

options:
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
